---
title: grepai init
description: CLI reference for grepai init
---

## grepai init

Initialize grepai in the current directory

### Synopsis

Initialize grepai by creating a .grepai directory with configuration.

This command will:
- Create .grepai/config.yaml with default settings
- Prompt for embedding provider (Ollama or OpenAI)
- Prompt for storage backend (GOB file or PostgreSQL)
- Add .grepai/ to .gitignore if present

```
grepai init [flags]
```

### Options

```
  -b, --backend string    Storage backend (gob, postgres, or qdrant)
      --engine string     Indexing engine: v1 (default, per-repo) or v2 (grepaid daemon)
  -h, --help              help for init
      --inherit           Inherit configuration from main worktree (for git worktrees)
  -m, --model string      Embedding model (for openai/openrouter: text-embedding-3-small, text-embedding-3-large; openrouter also supports qwen3-embedding-4b)
  -p, --provider string   Embedding provider (ollama, lmstudio, openai, synthetic, or openrouter)
      --ui                Run interactive Bubble Tea UI wizard
      --yes               Use defaults without prompting
```

### Git Worktree Support

When running `grepai init` inside a linked git worktree, grepai detects the main worktree and offers to inherit its configuration. Use `--inherit` to skip the prompt and automatically copy the config.

```bash
# Interactive: prompts to inherit
grepai init

# Non-interactive: inherit and skip all prompts
grepai init --inherit --yes
```

See [Git Worktrees](/grepai/git-worktrees/) for more details.

### SEE ALSO

* [grepai](/grepai/commands/grepai/)	 - Semantic code search CLI

###### Auto generated by spf13/cobra on 25-Jan-2026
